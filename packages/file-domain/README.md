# @repo/file-domain

Framework-agnostic file metadata and upload policy for Ossie.

This package owns reusable file-domain decisions only. Raw storage adapters, SQL, Fastify multipart parsing, and API response DTOs stay in app/server or shared API packages.
