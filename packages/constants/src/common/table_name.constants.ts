export enum Organization_Schema {
    SCHEMA = "organization_schema",
    Organization = "organization",
    Org_Role = "org_role",
}

export enum User_Schema {
    SCHEMA = "user_schema",
    User = "user",
    User_Asset = "user_asset",
}

export enum Auth_Schema {
    SCHEMA = "auth_schema",
    Auth_Session = "auth_session",
    Auth_Session_Init_Data = "auth_session_init_data",
    OTP_Verification = "otp_verification",
}

export enum Contact_Schema {
    SCHEMA = "contact_schema",
    Contact = "contact",
}
