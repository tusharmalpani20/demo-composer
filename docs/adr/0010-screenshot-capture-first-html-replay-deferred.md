# Screenshot Capture First HTML Replay Deferred

The MVP capture path is screenshot-based, with element metadata and bounding boxes for guide annotations and demo hotspots. HTML snapshots remain allowed in the schema but full HTML replay, sanitization, asset inlining, script stripping, and sandboxed interaction are deferred because they add significant security and rendering complexity before the internal guide workflow is proven.

