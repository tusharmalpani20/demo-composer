# Single Product Context With Domain Packages

Demo Composer v2 starts as one product context with separate domain packages rather than multiple bounded contexts. This keeps capture, guide, interactive demo, project, and publishing language consistent while the product model is still forming, while still giving implementation boundaries through packages such as `capture-domain`, `guide-domain`, `demo-domain`, and `publish-domain`.

