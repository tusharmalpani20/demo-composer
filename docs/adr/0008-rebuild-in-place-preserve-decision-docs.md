# Rebuild In Place And Preserve Decision Docs

Demo Composer v2 will be rebuilt in place while preserving `docs/`, `CONTEXT.md`, and ADRs. This avoids carrying old implementation assumptions into the new architecture while keeping the product and system-design decisions from the grill session as the source of truth.

