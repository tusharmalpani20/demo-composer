# Guide Blocks With First-Class Steps

Guides are modeled as ordered `guide_block` records, with first-class `guide_step` details for blocks whose type is `step`. This keeps insertion and reordering consistent for headers, tips, alerts, captures, and GIFs while preserving strong validation and behavior for the main numbered step concept.

