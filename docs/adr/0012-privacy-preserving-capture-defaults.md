# Privacy Preserving Capture Defaults

Capture defaults are privacy-preserving: raw typed input values are not stored by default, sensitive field types are redacted or omitted, and screenshot redaction creates derived assets rather than overwriting originals. This reduces the risk of accidentally storing secrets or customer data while still allowing users to manually write precise guide instructions after capture.

