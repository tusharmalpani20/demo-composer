interface:
  display_name: "Build Ossie Slice"
  short_description: "Deliver one child plan end to end"
  default_prompt: "Use $build-ossie-slice to expand, implement, verify, and close the active child plan."
